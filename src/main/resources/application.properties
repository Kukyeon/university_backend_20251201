spring.application.name=university_20251201

server.port=8888


#MySql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/universitydb
spring.datasource.username=root
spring.datasource.password=12345

spring.security.enabled=false


#JPA
spring.jpa.open-in-view=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.physical_naming_strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy

#react - CORS
spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS

#Gemini
gemini.api.key=AIzaSyD3H8LBWWFg9eNqNE9adBuj113Qm63vClM

#AWS-TEXT
# 1. AWS 자격 증명 (IAM 사용자 키 사용)
# 주의: 실제 배포 시에는 IAM Role을 사용해야 하며, 이 키는 환경 변수로 관리하는 것이 안전합니다.
cloud.aws.credentials.access-key=YOUR_AWS_ACCESS_KEY_ID
cloud.aws.credentials.secret-key=YOUR_AWS_SECRET_ACCESS_KEY

# 2. AWS 리전 설정 (사용하시는 리전으로 변경하세요)
cloud.aws.region.static=ap-northeast-2
cloud.aws.region.auto=false

# 3. Transcribe 결과가 저장될 SQS Queue 이름 (테스트용이므로 실제 AWS에 존재해야 합니다)
aws.sqs.transcribe-queue-name=your-transcribe-result-queue-name

file.upload-dir=images

spring.web.resources.static-locations=file:${file.upload-dir}/
